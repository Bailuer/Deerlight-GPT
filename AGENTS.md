# Deerlight GPT：教学与学习档案维护

这些规则适用于本项目的代码工作和围绕本项目的教学讨论。

## 开始与恢复学习

- 开始新会话、恢复中断的课程或上下文压缩后，先读 `LEARNING_LOG.md` 的“我们现在在哪”和“下一次怎么接上”；按需读相关章节和 `notes/`。
- 最新用户请求优先于旧学习计划。发现档案过时，按实际进度修正，不要求用户重复已学内容。
- 专有名词使用 English，解释使用中文；用具体数字、小例子分段推进。不要因用户说“OK”就认定整个模块已独立掌握。
- 代码讲解默认只选最关键的一块：说明目的、核心几行和一个重要易错点。不逐行讲完整文件，不展开普通 Syntax/辅助代码；用户追问时再展开。这是用户于 2026-09-13 明确提出的偏好。

## 更新频率：事件驱动，有节奏地检查

- 以下事件应在当前轮结束前更新：完成一个有实质内容的小节/模块；完成实验或重要代码验证；切换章节/学习路线；纠正重要误解；用户要求记录、总结或暂停学习。
- 没有上述事件时，每累计约 **5 个有实质内容的教学问答轮次**，检查一次是否需要合并更新。不计纯寒暄、重复确认或没有新进展的“OK”。
- 若连续约 **8 个实质教学轮次**仍未落档，应整理一个简短进度快照，不要把完整章节的过程一直留在聊天里。
- 以上是活跃对话内的检查节奏，不是后台计时任务。用户离开时不创建自动任务、不承诺后台更新。
- 不必每条消息都改文件；同一小节的解释、追问和澄清尽量合并记录。没有实质变化就不要制造更新。
- 每次实际更新后，给用户一句简短说明；不要用维护记录打断教学。维护本档案本身不要求每次重新征求许可。

## 更新位置和内容

- `LEARNING_LOG.md` 是进度的单一入口：保持顶部当前阶段、最近完成、限制、下一步，以及底部恢复指引相互一致。
- 详细概念笔记写入对应 `notes/` 文件；实验的配置与结果写入 `experiments/` 或引用本地运行产物。学习档案保留摘要和索引，不复制整段聊天或大量代码。
- 记录：实际日期、学了什么、用户表现出的理解/误解、谁实现了代码、验证结果、仍未验证的内容、一个明确下一步。
- 区分“讲解过”“有独立解释证据”“有独立实现证据”“有实验记录”。助手写完、用户阅读过的代码不能记成用户独立实现。
- 实验数据必须有可核实的输出/文件依据；注明配置、评估口径和局限。未知结果标待验证，不补造数字。
- 发生更正时注明更正，不把尚未执行的建议写成已完成。未完成的支线保留为待办，不让它盖过当前主线。
- 不在笔记中保存凭据、私人聊天全文或不必要的个人敏感信息。

## 工作边界

- 编辑前读取当前文件，保留用户及其他任务的已有改动。使用 `apply_patch`，改后检查 Markdown 和 diff。
- 档案维护不授权自动 Commit/Push、删除 Checkpoints、长训或扩大任务范围；这些按用户当次请求执行。
- 提交前若本次学习已有实质进展，先检查档案是否同步。`runs/`、数据与 Weights 被忽略时，说明本地文件不随 Git 上传。
